1. Optimize code, because my code is not efficient at all, but hey it works!
2. Remove magic values for strings and replace them with variables of some sort
3. Use Polyer some day. Maybe when it matures a bit more